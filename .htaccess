# Academia PayGas - Security

# Block access to sensitive files
<FilesMatch "^(\.env|\.git|\.htaccess|\.htpasswd)$">
    Require all denied
</FilesMatch>

# Block access to .env variants
<FilesMatch "\.env">
    Require all denied
</FilesMatch>
